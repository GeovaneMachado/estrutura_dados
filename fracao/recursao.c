#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<string.h>
#include"f_recursividade.h"


int main()
{
    //verificar prod;

    switch (menu())
    {
    case  1:
        printf("Opcao 1");
        break;   
    default:
        printf("Opcao Default");
        break;
    }
 //   printf("%i", MENU);
 /*  scanf("%s", &prod.nome);
    prod.tamanho = 180;
    prod.preco = 5.99;
   //codigo(&prod.codigo);
    printf("Nome: %s\nTamanho: %i\nCodigo: %i\nPreco:%.2lf", prod.nome, prod.tamanho, prod.codigo = 2, prod.preco);
    */return 0;
}